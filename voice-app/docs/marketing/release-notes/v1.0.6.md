# Release Notes v1.0.6 (2025-10-16)

<en-US>
- Solidified the mobile navbar collapse behavior so the menu stays hidden until toggled.
- Incremented Android build metadata to `versionCode 7` / `versionName 1.0.6` for the updated store package.
- Regenerated the Android App Bundle after the UI fix (`app/build/outputs/bundle/release/app-release.aab`).
</en-US>

<ko-KR>
- 모바일 네브바 접기 동작을 다시 손봐 토글을 누르기 전에는 메뉴가 열리지 않도록 고정했습니다.
- 스토어 배포를 위해 Android `versionCode`를 7, `versionName`을 1.0.6으로 올렸습니다.
- UI 수정 이후 Android App Bundle을 다시 생성했습니다 (`app/build/outputs/bundle/release/app-release.aab`).
</ko-KR>